Awesome — here’s a copy‑paste prompt you can drop into Builder.io’s “Generate” to create the NeonSense Crypto Dashboard.

---

# Prompt for Builder.io (NeonSense Crypto Dashboard)

Design a **crypto dashboard page** for **NeonSense** with a sleek neon–cyberpunk theme (dark UI, subtle glassmorphism, neon accents, soft glows, smooth micro‑animations). Build with a clean, component‑driven layout and responsive behavior (desktop first, then tablet/mobile).

## Brand & Theme

* **Palette:**

  * Background: `#0A0F1F` (page), `#0F152B` (cards)
  * Accents: Neon teal `#00E5FF`, magenta `#FF1F8F`, lime `#C3FF00`
  * Success: `#16C784`, Danger: `#EA3943`, Neutral: `#9AA4BF`
* **Typography:** Inter or Urbanist. Headings semibold; body regular. Tight letter‑spacing for numerals.
* **Style:** Rounded‑2xl cards, soft shadows, 1px inner strokes, glass blur on hero. Animate hover/focus, number tickers, and gauge sweeps.

## Page Structure (top → bottom)

1. **Top App Bar**

   * Left: NeonSense logo, “Crypto Dashboard”
   * Center: Global search (tokens, wallets, news)
   * Right: Wallet connect button, notifications bell, user avatar menu
   * Sticky on scroll; shrink height with slight blur.

2. **Hero Overview**

   * Card group (4 up on desktop, 2 up tablet, 1 up mobile):

     * **Total Portfolio** (USD + BTC/ETH equivalent) with tiny sparkline
     * **24h P/L** with animated green/red pulse
     * **Win Rate (7d)** with mini radial
     * **Quick Trade** (symbol input, amount, Buy/Sell buttons)
   * Subtle gradient glow backdrop.

3. **Market Pulse Row**

   * **AI Sentiment Gauge** (0–100; labels: Fear–Neutral–Greed). Color sweep red→yellow→green.
   * **Trending Coins Heatmap** (10–20 tiles): each tile shows symbol, price, 24h %, volume bar; brighter glow for high momentum. Click tile → opens Coin Drawer.
   * **News Ticker** marquee of top 6 headlines; hover to pause; click opens news modal.

4. **Watchlists + Charts**

   * **Watchlist Tabs:** “All”, “Blue Chips”, “DeFi”, “Memes”, “Custom”
   * Table: symbol, price, 24h %, 7d %, market cap, volume, alert bell toggle
   * **Advanced Chart Panel** beside table (on desktop): TradingView‑style line/candles with overlays toggles: Sentiment, Volume Spikes, Whale Flow. Comparison mode (BTC vs ETH).
   * On mobile: chart collapses under table with a “Show Chart” button.

5. **On‑Chain Activity Radar**

   * Real‑time feed: large transfers, new token launches, DEX swaps.
   * Badge color: Buy (success), Sell (danger), Transfer (neutral).
   * Filter chips: Size ≥ \$100k, New Pair, Smart Money.

6. **AI “Neon Brief”**

   * Card with 3–5 bullets: biggest movers, key news, pattern setup ideas.
   * “Refresh Insights” button with loading shimmer.

7. **Portfolio Health**

   * Score (A–F) and meter based on volatility, diversification, and market trend.
   * Suggestions list (e.g., “Reduce exposure to high‑beta assets”).
   * “Simulate Rebalance” CTA opens a modal with hypothetical P/L.

8. **Gamification**

   * Badges carousel (e.g., +10% Day, 1 BTC Holder, First DeFi Stake).
   * “Daily Gains Leaderboard” mini‑table.

9. **Footer**

   * Links: Docs, API, Status, Terms, Privacy. Social icons with neon hover.

## Components (make reusable)

* `NS/Card`, `NS/StatTile`, `NS/Badge`, `NS/Chip`, `NS/TabBar`, `NS/Table`, `NS/HeatmapTile`, `NS/Gauge`, `NS/Sparkline`, `NS/Modal`, `NS/Drawer`, `NS/Toast`
* Variants: default / loading (skeleton) / error (retry)

## Interactions

* Heatmap tile click → **Coin Drawer** (right side): price, 24h/7d chart, order buttons, latest tweets/news, whale tracker.
* Alert bell toggle → toast “Price alert set for {coin}”.
* Chart overlay toggles (checkboxes) update chart layers without reloading.
* News ticker item opens **News Modal** with headline, source, timestamp, summary.
* “Simulate Rebalance” modal: sliders per asset, recalculated score and projected volatility.
* Persistent user prefs (theme, overlays, default watchlist).

## Data (bind with placeholders now)

Use Builder data bindings or JSON data models with these fields. Populate with sample data; mark as dynamic:

* `portfolio`: { totalUsd, totalBtc, totalEth, pnl24hPct, winRate7d }
* `watchlists[]`: { id, name, symbols\[] }
* `tokens[]`: { symbol, name, price, change24h, change7d, mcap, vol24h, sparkline\[] }
* `sentiment`: { score0to100, label }
* `heatmap[]`: { symbol, price, change24h, volume, momentumScore }
* `onchain[]`: { time, type, symbol, usdValue, fromAddr, toAddr, txHash, dex? }
* `news[]`: { title, source, url, time, summary }
* `neonBrief[]`: { bullet }
* `portfolioHealth`: { grade, score, suggestions\[] }

## Responsive Rules

* Desktop: 12‑col grid, 24px gaps.
* Tablet: 8‑col grid, stack chart under table.
* Mobile: single column; collapse complex panels behind accordions.

## Accessibility

* High‑contrast text, focus outlines, ARIA roles for tabs/tables/modals.
* Number readers: `aria-live="polite"` for price/P\&L changes.

## Performance

* Lazy‑load charts and modals; skeleton loaders.
* Virtualize long tables/feeds.
* Sprite or inline minimal icons; defer heavy libs until interaction.

## Micro‑animations

* Number ticker for prices/P\&L (ease‑out 250ms).
* Gauge sweep (750ms).
* Heatmap glow pulse on momentum spikes (subtle).

## Empty / Error States

* Empty watchlist: “No coins yet — add from Trending.”
* Network error: retry button + minimal error copy.
* No data: show skeletons.

## Pages/Routes (optional)

* `/dashboard` (this page)
* `/coin/[symbol]` (detail page scaffold)
* `/portfolio` (future)

**Deliverables:** Generate this as a single Builder.io page with reusable components and placeholder data wired to bindings (easy to swap for APIs later). Match the neon cyberpunk aesthetic and keep the layout clean, responsive, and production‑ready.

---

Want me to plug in real sample JSON so your heatmap, table, and gauges animate inside Builder right away?
